import React from 'react';
import { shallow } from 'enzyme';
import renderer from 'react-test-renderer';
import Home from '../components/Home';

// test('it should add', () => {
//   const component = shallow(<App />);
//   expect(component).toEqual(3);
// });

// describe('testing', () => {
//   it('should do stuff', () => {
//     const wrapper = renderer.create(<Home />).toJSON();
//     expect(wrapper).toMatchSnapshot();
//   });
// });
// describe('testing', () => {
//   it('showSignIn should default to false', () => {
//     const wrapper = shallow(<DiscoverCard />);
//     const showSign = wrapper.find('.footer-btn').text();
//     expect(showSign).toEqual('Get Started!');
//   });
// });

// describe('testing', () => {
//   it('should do stuff', () => {
//     const wrapper = 3;
//     expect(wrapper).toEqual(2);
//   });
// });
